# Dependencies — never commit these
node_modules/

# Sensitive local-dev files — contain API keys / secrets
server.py
generate-escrow.js
LAUNCH.bat
redeploy.ps1
*.log

# Private key / seed phrase / wallet files — NEVER commit these
*.txt
private*

# Environment files
.env
.env.local
.env.*.local

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.swo

# Vercel & Claude local config (machine-specific, not needed in repo)
.vercel
.claude

.vercel

# Discord bot token for clip fetching — NEVER commit
scripts/discord_token.txt
discord_clips/

# clip-montage working files (keep out of git)
scratch_frames/
scratch_sheets/
scratch_cut/
scratch_*.js
scratch_*.jpg
promo/
scratch_strips/
scratch_cut2/
scratch_vert/
